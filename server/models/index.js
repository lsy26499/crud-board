const user = require('./user');
const board = require('./board');
const product = require('./product');
const order = require('./order');

const models = { user, board, product, order };

module.exports = models;
